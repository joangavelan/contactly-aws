{
  "name": "contactly-aws",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "infra",
    "packages/*"
  ],
  "scripts": {
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.16"
  }
}
